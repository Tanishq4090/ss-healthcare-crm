import https from 'https';

const SUPABASE_URL = "https://sgyladamwnanudnropwl.supabase.co";

async function run() {
    console.log("Checking if we can delete ElevenLabs calls...");
    // we don't have the API key locally, it's in the edge function environment vars
}
run();
